# Install Service

sudo nano /etc/systemd/system/led-blinker.service

# Recargar systemd

sudo systemctl daemon-reload

# Start Service

sudo systemctl start led-blinker

# Hacer que el servicio se inicie con el arranque

sudo systemctl enable led-blinker